import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { router } from 'expo-router';
import { useGetVendor } from '@workspace/api-client-react';
import { useColors } from '@/hooks/useColors';
import { GhostButton, SectionLabel } from '@/components/ui';
export default function Profile() { const c = useColors(); const vendor = useGetVendor(1); const v = vendor.data; const rows = [['Business', v?.businessName || 'ABC Car Rentals'], ['Contact', v?.contactName || ''], ['Phone', v?.phone || ''], ['WhatsApp', v?.whatsappNumber || ''], ['Email', v?.email || 'Not provided'], ['Pick-up areas', v?.pickupLocations?.join(', ') || 'Not set']]; return <ScrollView style={[styles.screen, { backgroundColor: c.background }]} contentContainerStyle={styles.content}><Text style={[styles.title, { color: c.foreground }]}>Business profile</Text><Text style={[styles.copy, { color: c.mutedForeground }]}>Your public vendor details on QuickHire.</Text><SectionLabel>Profile details</SectionLabel><View style={[styles.card, { backgroundColor: c.card, borderColor: c.border }]}>{rows.map((row, i) => <View key={row[0]} style={[styles.row, { borderBottomColor: c.border }, i === rows.length - 1 && styles.last]}><Text style={[styles.label, { color: c.mutedForeground }]}>{row[0]}</Text><Text style={[styles.value, { color: c.foreground }]}>{row[1]}</Text></View>)}</View><GhostButton title="Back to account" icon="arrow-left" onPress={() => router.back()} /></ScrollView>; }
const styles = StyleSheet.create({ screen: { flex: 1 }, content: { padding: 20, paddingTop: 36, gap: 12 }, title: { fontSize: 27, fontWeight: '700', letterSpacing: -0.6 }, copy: { fontSize: 14, marginBottom: 18 }, card: { borderWidth: 1, borderRadius: 18, overflow: 'hidden', marginBottom: 10 }, row: { minHeight: 62, paddingHorizontal: 15, paddingVertical: 11, borderBottomWidth: 1, justifyContent: 'center' }, last: { borderBottomWidth: 0 }, label: { fontSize: 11, fontWeight: '700', marginBottom: 3 }, value: { fontSize: 14, fontWeight: '600' } });
